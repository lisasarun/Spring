spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/a01_ecommerce_db
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  application:
    name: webmvc
server:
  port: 8090
  servlet:
    context-path: /istad